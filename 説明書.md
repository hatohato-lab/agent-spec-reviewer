# 説明書 — agent-spec-reviewer

この文書は、IT や AI に詳しくない方に向けて、このリポジトリ（プログラムと文書の置き場）が
何をするものかを、専門用語をできるだけ使わずに説明するものです。

## 一言でいうと

AI に「設定ファイルの点検」をさせ、その点検が信頼できるかどうかを、
答えのあらかじめ分かっている見本でテストする仕組みです。

## 何の役に立つのか

Claude Code という AI ツールでは、AI への指示を小さな設定ファイルに書いて使います。
この設定ファイルには決まった書き方があり、書き方を間違えると正しく動きません。
また、昔は必要だった工夫が、今の AI では不要になっている（古くなっている）こともあります。

このリポジトリの AI（査読エージェント）は、そうした設定ファイルを点検し、
「決まりどおりに書けているか」「古くなった記述が残っていないか」を報告します。
人が資料を読み比べて確かめる手間を減らすことが目的です。

## 正しさをどう確かめているか

AI の点検には、人間の点検と同じく、見落としや言いすぎ
（実際には無い問題まで報告してしまうこと）が起こりえます。
そこで、このリポジトリでは次の方法で点検の正しさを測っています。

たとえるなら、答案の採点者が信頼できるかを確かめるために、
わざと間違いを入れた答案を混ぜて採点させるようなものです。

1. 「どこに問題を仕込んだか」があらかじめ分かっている見本ファイルを用意します。
   問題のない正しい見本も 1 つ混ぜます。
2. AI にそれらを点検させ、報告を受け取ります。
3. 採点プログラム（オラクル）が、報告と正解を突き合わせます。
   仕込んだ問題をすべて見つけ、余計な問題をでっち上げず、
   正しい見本を正しいと判定できたときだけ「合格」とします。

さらに、採点プログラム自身も検証しています。
わざと欠陥のある報告を 3 種類用意し
（何も見つけない報告・浅くしか見ない報告・何でもかんでも問題だと書き並べる報告）、
採点プログラムがそれらをきちんと「不合格」にできることを確認しています。
この確認は `python eval/oracle.py --selftest` という 1 つの命令で、いつでも再実行できます。

## フォルダとファイルの案内

- `README.md` … 利用者向けの案内。実行方法と全体のしくみが書かれています。
- `説明書.md` … この文書です。
- `.claude/agents/` … 点検を行う AI への指示書が入っています。
- `eval/corpus/` … 点検させる見本ファイル。正しいもの 1 つと、問題を仕込んだもの 4 つです。
- `eval/selftest/` … 採点プログラム自身を検証するための、報告のサンプルです。
- `eval/oracle.py` … 採点プログラムの本体です。
- `design/design.md` … なぜこの作りにしたか、という設計の記録です。
- `.github/workflows/` … 変更のたびに上記の検証を自動で走らせるための設定です。

## 用語の小辞典

- エージェント … 指示を受けて作業を行う AI のこと。
- 査読 … 文書を点検して、問題があれば指摘すること。
- オラクル … 結果が正しいかどうかを機械的に判定する、採点プログラムのこと。
- 見本（コーパス） … テストのために用意した、答えの分かっているファイル一式のこと。
- 陳腐化 … 昔は意味があったが、今では不要・時代遅れになっていること。
